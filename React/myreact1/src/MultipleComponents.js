function MultipleComponents() {
  // This task's starting point is the App component's h1 element that reads: "Task: Add three Card elements".
  return <h1>Task: Add three Card elements</h1>;
}

export default MultipleComponents;
